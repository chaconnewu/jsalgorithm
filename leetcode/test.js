var Person = function() {
    var age = 25;

    // this.getAge = function() {
    //     return age;
    // };
}

var person = new Person();
// console.log(person.getAge());

console.log(person.age);
